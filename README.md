This project is `$GOPATH` directory

- Run `make` (or `src/*.bash` files)
- Run `etc/gen.envl.bash` in case overriding some env vars
- Run `src/vendor/update.bash` in case updating external dependencies
