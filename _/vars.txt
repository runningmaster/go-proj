#!/bin/sh

# Don't override!
export ROOT=$(dirname `pwd`)
export EXT=$ROOT/ext
export SRC=$ROOT/src
export OUT=$ROOT/.out

# Override ==============================
export APP=app.out
export GOROOT=$HOME/Development/go
# =======================================

# Don't override!
export GOPATH=$EXT
export PATH=$GOROOT/bin:$GOPATH/bin:$PATH
