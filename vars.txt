PROJ_HOST="github.com/runningmaster"  # ready for override
PROJ_NAME="ini403d"                   # ready for override
PROJ_PATH=$PROJ_HOST/$PROJ_NAME 

export GOROOT=$HOME/Development/go    # ready for override
export GOPATH=$(pwd)/ext
export PATH=$GOROOT/bin:$PATH

if [ ! -h $GOPATH/src/$PROJ_PATH ]; then
	mkdir -p $GOPATH/src/$PROJ_HOST
	ln -s ${PWD}/src $GOPATH/src/$PROJ_PATH
fi

eval $(go env)
