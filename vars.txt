proj_name="project403"                # override if necessary
proj_path="github.com/runningmaster"  # override if necessary

export GOROOT=$HOME/Development/go    # override if necessary
export GOPATH=$(pwd)/ext
export PATH=$GOROOT/bin:$GOPATH/bin:$PATH
